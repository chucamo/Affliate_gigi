import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

export default function Products_Affiliate({name}) {
  return (
    <View>
      <Text>{name}</Text>
    </View>
  )
}

const styles = StyleSheet.create({})